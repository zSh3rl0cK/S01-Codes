package main

import (
	"fmt"
	"math/rand"
)

func main() {
	x:= rand.Intn(10)
	
	result:= 1
	
	for i := x; i > 0; i-- {
		result = result * i
	}

	fmt.Printf("O fatorial do %d número é: %d\n", x, result)
}	
